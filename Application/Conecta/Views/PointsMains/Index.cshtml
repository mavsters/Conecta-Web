@model IEnumerable<Conecta.Models.Points.PointsMain>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.count)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QRMain)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.count)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QRMain.QRMainId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PointsMainId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PointsMainId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PointsMainId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
