@model IEnumerable<Conecta.Models.Events.UserMain_Event>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attend)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserMain)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Attend)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserMain.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserMain_EventId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserMain_EventId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserMain_EventId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
