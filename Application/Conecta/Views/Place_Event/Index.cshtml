@model IEnumerable<Conecta.Models.Events.Place_Event>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Place)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Event.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Place.PlaceId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Place_EventId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Place_EventId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Place_EventId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
