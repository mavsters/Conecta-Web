@model IEnumerable<Conecta.Models.CountryStructure.Country>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header card-header-primary card-header-icon">
                <div class="card-icon">
                    <i class="material-icons">assignment</i>
                </div>
                <h4 class="card-title">Lista de Paises</h4>
                <a asp-action="Create">Crear uno nuevo</a>
            </div>
            <div class="card-body">
                <div class="toolbar">
                    <!--        Here you can write extra buttons/actions for the toolbar              -->
                </div>
                <div class="material-datatables">
                    <div id="datatables_wrapper" class="dataTables_wrapper dt-bootstrap4">
                        <div class="row">
                            <div class="col-sm-12">
                                <table id="datatables" class="table table-striped table-no-bordered table-hover dataTable dtr-inline" cellspacing="0" width="100%" style="width: 100%;" role="grid" aria-describedby="datatables_info">
                                    <thead>
                                        <tr role="row">
                                            <th class="sorting" tabindex="0" aria-controls="datatables" rowspan="1" colspan="1" style="width: 423px;" aria-label="Position: activate to sort column ascending">@Html.DisplayNameFor(model => model.Name)</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatables" rowspan="1" colspan="1" style="width: 423px;" aria-label="Position: activate to sort column ascending">@Html.DisplayNameFor(model => model.IATA)</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatables" rowspan="1" colspan="1" style="width: 423px;" aria-label="Position: activate to sort column ascending">@Html.DisplayNameFor(model => model.CountryFlagUrl)</th>
                                            <th class="disabled-sorting text-right sorting" tabindex="0" aria-controls="datatables" rowspan="1" colspan="1" style="width: 325px;" aria-label="Actions: activate to sort column ascending">Actions</th>
                                        </tr>
                                    </thead>
                                    <tfoot>
                                        <tr><th rowspan="1" colspan="1">@Html.DisplayNameFor(model => model.Name)</th><th rowspan="1" colspan="1">@Html.DisplayNameFor(model => model.IATA)</th><th rowspan="1" colspan="1">@Html.DisplayNameFor(model => model.CountryFlagUrl)</th><th class="text-right" rowspan="1" colspan="1">Actions</th></tr>
                                    </tfoot>
                                    <tbody>
                                        @{ var count = 0;}
                                        @foreach (var item in Model)
                                        {
                                            <tr role="row" class="@(((count++)%2==0)?"odd":"even")">
                                                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.IATA)
                                                </td>
                                                <td>
                                                    <img src="@Html.DisplayFor(modelItem => item.CountryFlagUrl)" alt="@Html.DisplayFor(modelItem => item.CountryFlagUrl)" style="width:100px"/>
                                                </td>
                                                <td class="text-right">
                                                    <a asp-action="Edit" asp-route-id="@item.CountryId" class="btn btn-link btn-warning btn-just-icon edit">Edit</a>
                                                    <a asp-action="Details" asp-route-id="@item.CountryId" class="btn btn-link btn-info btn-just-icon like">Details</a>
                                                    <a asp-action="Delete" asp-route-id="@item.CountryId" class="btn btn-link btn-danger btn-just-icon remove">Delete</a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- end content-->
        </div>
        <!--  end card  -->
    </div>
    <!-- end col-md-12 -->
</div>
